# backend (Medusa)

Medusa v2.19 commerce backend + admin. Part of [medusa-showcase](../) — see root [`AGENTS.md`](../AGENTS.md) for Zerops topology, siblings, and import YAML location ([`zeropsio/recipes/medusa-showcase`](https://github.com/zeropsio/recipes/tree/main/medusa-showcase)).

- Hostname / `zeropsSetup`: `medusa` — port `9000` (`/health`, admin `/app`)
- Setup in root [`zerops.yml`](../zerops.yml) — `cd backend` for build/run
- Dev: `yarn dev` from this directory
